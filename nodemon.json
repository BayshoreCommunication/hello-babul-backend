{
  "watch": [".", "src", "config", "routes", "controller", "modal", "middleware", "utils"],
  "ext": "ts,json",
  "ignore": ["src/**/*.spec.ts", "node_modules", "dist", "*.log"],
  "exec": "ts-node server.ts",
  "legacyWatch": true,
  "env": {
    "NODE_ENV": "development"
  }
}

